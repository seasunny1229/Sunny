<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seasunny.sunny.resource.mapper.ExcelRowDataMapper">

    <insert id="insertRow">
        insert into excel_row_data(excel_name, sheet_name, created_time)
        values(#{excelRowData.excelName},#{excelRowData.sheetName}, now())
    </insert>

    
</mapper>